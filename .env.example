# Copy to .env for local overrides. Relative paths resolve from the project root.
DATA_DIR=.data

# Defaults to $DATA_DIR/uv-cache.
# UV_CACHE_DIR=

# Leave unset or use auto. The shared bootstrap selects copy only across filesystems.
# UV_LINK_MODE=auto
