"""Cross-service message contracts for the ss services.

Contracts are authored as ODCS v3 YAML and generated to Pydantic models committed under
`ss_contracts.models`, so a git-tag install needs no generation step.
"""

DISTRIBUTION = "ss-common"
