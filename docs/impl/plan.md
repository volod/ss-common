# ss-common Implementation Plan

Forward-only: this file describes work that remains. Available behavior belongs in
[current-state documentation](current.md); product behavior and evaluation belong in the
[specification](../design/spec.md). Task fields, statuses, and ordering are defined in the
[planning workflow](../guide/planning-workflow.md). Run `make plan-status` for counts and the
next eligible task.

While ss-common is staged, its forward work lives in the staging repository's plan (see
[Staged work](../design/spec.md#staged-work)) and moves here on publish.

## Agent Implementation Tasks

No open agent tasks.

## Human-Assisted Tasks

No open human-gated tasks.
